# Cardano Ledger Oracle Memory

## Meta
- [oracle_ledger_*.md KB table files referenced in this agent's system prompt do not exist — use live GitHub via `gh api` instead](kb-table-files-missing-use-live-github.md) — checked 2026-07-07, confirmed missing repo-wide; `gh` is authenticated and works for search/code + contents fetch

## STS Framework / Trusted Replay Mechanics
- [ValidateNone/reapplySTS Predicate-skip mechanics (live-verified 2026-07-07)](reapply-validatenone-predicate-skip-mechanics.md) — `Predicate cond orElse val -> ValidateNone: pure val` skips EVERY `?!`/`runTest`/`failOnNonEmptyMap` check in EVERY rule during trusted block reapply (`applyBlockNoValidaton`); state transitions (e.g. `drainAccounts`) are structurally independent of Predicate clauses and always run in full; worked example = Conway/Shelley withdrawal exact-match + DRep-delegation checks, both safely skippable because `drainAccounts` unconditionally zeroes (not conditionally subtracts)

## Reward Calculation (Byte-Exact)
- [Reward formula 3-stage floor chain + sigma vs sigmaA distinction (live-verified 2026-07-07)](reward-calc-floor-chain-and-sigma-vs-sigmaA.md) — maxPool'->poolR->leader/member, each an independent `rationalToCoinViaFloor`; sigma(totalStake-normalized, reward-share) vs sigmaA(totalActiveStake-normalized, apparent-performance-ONLY) are DISTINCT and easy to conflate; a0/nOpt/d/rho/tau all from PREVIOUS epoch's PParams; pledge-gate uses "go"-snapshot values w/ `<=`; member-registration filtering happens AFTER the floor, era-gated by hardforkBabbageForgoRewardPrefilter

## CBOR Structure Reference
- [NewEpochState/EpochState/LedgerState/UTxOState complete encoding](newepochstate-complete-encoding.md) — verified field order, exact array sizes, key warnings
- [Conway PParams array(31) field order](conway-pparams-field-order.md) — all 31 fields indexed 0-30, driven by eraPParams list
- [Conway CertState/DState/PState/VState encoding](conway-certstate-encoding.md) — array sizes, field order, StakePoolState vs StakePoolParams
- [SnapShots new vs old format](snapshots-encoding.md) — array(2) new format, array(3) old, StakePoolSnapShot array(10)
- [ConwayGovState encoding](conway-gov-state-encoding-detailed.md) — array(7), nested types
- [Conway Accounts/ConwayAccountState encoding](conway-accounts-encoding.md) — per-account array(4) with nullable delegations
- [OutputTooBigUTxO/maxValSize exact mechanics (live-verified 2026-07-31)](outputtoobigutxo-maxvalsize-exact-mechanics.md) — Conway reuses Alonzo.validateOutputTooBigUTxO unchanged via Babbage; measures a FRESH re-encode of decoded Value (never wire bytes, contrast Sized's real ByteSpan used by min-UTxO check); encodeMap definite(<=23 entries)/indefinite(>23) threshold, byte-neutral for N in 24-255; ada-only=bare coin int no array; strict `>` comparison

## JSON Debug-Dump (aeson ToJSON) Field Names — separate from CBOR wire format
- [PPUPState/ProposedPPUpdates/PParamsUpdate JSON keys + Conway "ppups" replacement (live-verified 2026-07-06)](ppup-json-field-names-debug-dump.md) — ShelleyGovState->proposals/futureProposals/curPParams/prevPParams; ProposedPPUpdates is array-of-pairs NOT object; PParamsUpdate keys are data-driven ppName (txFeePerByte not minFeeA, etc, full table); Conway "ppups" key survives but renders ConwayGovState (currentPParams/previousPParams renamed, no futureProposals, proposals=GovActionState list)

## Script Hashing (native/Timelock, Plutus)
- [hashScript/SafeToHash/MemoBytes native-script hashing exact mechanics (live-verified 2026-07-06)](native-script-hash-memobytes-safetohash.md) — hashScript = prefixTag<>originalBytes, NEVER a re-encode; originalBytes from MemoBytes.mbBytes captured via Annotator/withSlice byte-span at decode time; Timelock decoder tolerates BOTH definite/indefinite outer array (decodeListLenOrIndef) + non-minimal SlotNo ints (plain decodeWord64, not canonical variant); witness-set vs TxOut reference-script (tag24 bstr-wrapped) hashing is the SAME mechanism, AlonzoScript's hand-written SafeToHash bypasses its own [0,<bytes>] sum-wrapper; prefix bytes native=0x00/V1=0x01/V2=0x02/V3=0x03/V4=0x04(unused)
- [Dugite native-script hash audit: 2 confirmed bugs 2026-07-06](project_dugite_native_script_hash_audit_2026_07_06.md) — NOT FIXED: (1) NativeScript enum has no raw-byte capture, native_script_hash()/compute_script_ref_hash() re-encode canonically via encode_native_script instead of hashing original wire bytes; (2) era_shelley.rs+era_alonzo.rs read_native_script HARD-REJECT indefinite-length outer array (era_conway.rs's copy already fixed, 3 duplicate impls total, needs dedup)

## V3 ScriptContext / Plutus Data Encoding
- [ChangedParameters (PParamsUpdate) Plutus Data encoding](changed-parameters-plutus-data-encoding.md) — Data::Map keyed by ppuTag integers; all value shapes; Rational=List NOT Constr 0; CostModels/voting thresholds

## V1/V2 TxInfo Translation Gates (Conway + Babbage) — NOT in static KB, escalate to cardano-haskell-oracle
- [ConwayContextError/BabbageContextError exact gates, live-verified 2026-07-06](v1v2-txinfo-conway-babbage-gates.md) — guardConwayFeaturesForPlutusV1V2 fires on BOTH V1+V2 (voting/proposal non-empty, currentTreasuryValue SJust, treasuryDonation!=0 value-gated not presence-gated); ConwayVoting/ConwayProposing purpose -> PlutusPurposeNotSupported catch-all; ALL failures are whole-tx CollectErrors (pre-IsValid hard reject), never per-script skip; V1/V2 CAN legally hold governance-purpose credentials on-chain (no Phase-1 language gate); Conway's OWN PlutusV1 TxInfo instance drops Babbage's ReferenceInputsNotSupported check entirely (unconditional from PV9, NOT the same as the PV11 disjointness relaxation)
- [PlutusV3 unit-return check + Byron-addr Alonzo-drop/Babbage-error + StakingPtr passthrough (live-verified 2026-07-06)](plutus-txinfo-translation-v3unit-byron-pointer.md) — unit-check is language-gated (ll≠V1/V2) not PV-gated, live since before Chang#1; Alonzo silently drops Byron TxOuts (documented "mistake"), Babbage+/Conway hard-error via ByronTxOutInContext (era-gated not language-gated); transAddr/StakingPtr is ONE shared fn reused unchanged Alonzo→Conway
- [Dugite Plutus-context audit: 2 divergences + 2 confirmed-correct, 2026-07-06](project_dugite_plutus_context_audit_2026_07_06.md) — NOT FIXED: rationals not gcd-reduced before ToPlutusData (dugite reader.rs::read_rational); Byron-addr hard-error applied to ALL eras incl. Alonzo (should drop-only pre-Babbage). CONFIRMED CORRECT (no change): V3 unit-return check, StakingPtr passthrough
- [Certifying/Rewarding/Voting/Proposing witness matrix + scriptsNeeded indexing + guardrails-hash equality check (live-verified 2026-08-02)](conway-plutus-purpose-witness-and-indexing.md) — full per-TxCert-constructor witness table (ONLY `ConwayRegCert cred SNothing` is permissionless; the Conway explicit-deposit reg form and the combined RegDelegCert are both ALWAYS witnessed); getConwayScriptsNeeded's 6 sub-builders + which container each indexes (Certifying/Proposing = submission-order OSet, Rewarding/Voting = Ord-sorted Map.keys, AccountAddress sorts network-then-credential, Voter sorts CommitteeVoter<DRepVoter<StakePoolVoter); ConwayPlutusPurpose wire tags 0-5 confirmed exact; ParameterChange/TreasuryWithdrawals guardrails hash is a STRICT EQUALITY check (`InvalidGuardrailsScriptHash`) against the CURRENT constitution's script — an arbitrary always-true script is invalid, must match exactly; hidden `CertificateNotSupported` sub-restriction means V1/V2 Certifying scripts ONLY work on plain stake_delegation/reg_deposit_cert/unreg_cert, NOT DRep/CC/RegDelegCert/vote-delegation certs (those need V3, independent of the Voting/Proposing purpose-level V3-only gate)

## Governance / Epoch Boundary Corrections
- [Conway proposal deposit epoch boundary — verified source facts](feedback_proposal_deposit_epoch_boundary.md) — returnProposalDeposits scope, expiry off-by-one, no silent drops, divergence diagnosis
- [DRep expiry and vsNumDormantEpochs mechanics](drep-expiry-numDormantEpochs.md) — computeDRepExpiry formula, cumulative counter, delta-only correction, expiry check reCurrentEpoch > drepExpiry
- [Conway RATIFY/GOV precision facts (live-verified 2026-07-04)](conway-ratify-precision-facts.md) — pvCanFollow exact modulus, HF proposal chaining (point 2 SUPERSEDED, see below), reorderActions stability, ensTreasury NOT stale, RatifyEnv frozen fields, committee minSize-before-zero-threshold gate, returnProposalDeposits unclaimed->treasury, rsDelayed mid-pass fold
- [computeDRepDistr exact composition + deposit attribution (live-verified 2026-07-26)](drep-distr-deposit-attribution.md) — DRep/gov-SPO stake = InstantStake[cred]+ProposalDeposits[cred]+AccountBalance[cred] per delegating credential; proposal deposits keyed by RETURN-ADDRESS credential (gasReturnAddrL), summed across proposals sharing one return addr; ppKeyDeposit + ppDRepDeposit + ppPoolDeposit ALL excluded (separate fields/namespaces, never read by computeDRepDistr); governance poolDistr (from mark snapshot) gets proposal deposits added, leader-schedule/reward poolDistr (from go snapshot) does NOT
- [pvCanFollow/preceedingHardFork/ProposalCantFollow byte-exact mechanics (raw-source-verified 2026-07-06)](hardfork-pvcanfollow-exact-mechanics.md) — full verbatim Gov.hs:673-694 + 483-499 + 193-199; 3-way base resolution (root / succVersion-shortcut-forces-current / in-flight-parent-target); corrects a wrong worked example in conway-ratify-precision-facts.md #2; GOV=block-apply time not RATIFY/ENACT; no bootstrap special-casing
- [Dugite ratify audit: 3 open divergences found 2026-07-04](project_dugite_ratify_audit_divergences_2026_07_04.md) — NOT FIXED: pv_can_follow accepts any minor>cur (not cur+1); HF chaining ignores in-flight preceding proposal's target version (divergence-2 example corrected 2026-07-06, see hardfork-pvcanfollow-exact-mechanics.md); committee zero-threshold auto-pass fires before minSize gate
- [BoundedRatio decode bounds + Conway ENACT totality (live-verified 2026-07-06)](bounded-ratio-decode-and-enact-totality.md) — UnitInterval CBOR decode structurally rejects num>den (boundRational, DecoderErrorCustom); NonNegativeInterval (a0) has NO upper bound; ENACT PredicateFailure=Void, applyPPUpdates is total, no re-validation at enactment
- [MIR pot-transfer exact semantics (live-verified 2026-07-06)](mir-pot-transfer-semantics.md) — standalone Mir.hs mirTransition at NEWEPOCH boundary; delta formula is own-pot-only (NOT cross-swapped, cross semantics baked into sign at DELEG-time); solvency = single combined AND over both pots; PredicateFailure=Void (total, never fails); insolvency = both pots unchanged + pending maps always wiped, dropped forever; registered-cred filter via Map.intersection at MIR boundary; Conway deletes MIR call site + TxCert constructor entirely (not PV-gated)
- [Shelley PPUP/NEWPP votedFuturePParams RESTORED + live-verified 2026-07-06](shelley-ppup-votedfutureparams-verified.md) — supersedes old broken-link summary below (PPUP half only): tally keyed by whole PParamsUpdate value (byte/structural identity), `[ppu]<-Map.keys consensus` pattern-match fails to Nothing on 0-or-2+ (no merge, ever); guard is INSIDE votedFuturePParams on merged ppNew, strict `<`; quorum = fixed `sgUpdateQuorum` genesis constant (not computed), load-time-validated as strict majority of sgGenDelegs
- [Dugite issue #784 PPUP quorum bug: 6 call sites bypassed voted_future_pparams (found+fixed 2026-07-06)](project_dugite_issue_784_ppup_quorum_fix_2026_07_06.md) — bug counted DISTINCT PROPOSERS (any value) + last-writer-merged every field across ALL proposals, instead of requiring one byte-identical value at quorum; sites: state/epoch.rs, eras/shelley.rs, eras/conway.rs, state/mod.rs x3 forecast fns; fix (found already in-progress, uncommitted) adds `fold_pp_proposals` and routes all 6 through the pre-existing correct `voted_future_pparams`; residual unverified: Vec submission-order invariant that `fold_pp_proposals` relies on

## Shelley/Byron/Alonzo Deep-Dive Facts (live GitHub-verified 2026-07-04)
- **BROKEN LINKS as of 2026-07-06**: the entries below were indexed on 2026-07-04 but their files do NOT exist on disk (never persisted, or lost). Do not cite the one-line summaries as verified fact — re-derive live from GitHub before relying on any of them. Restored so far: `byron-duplicate-inputs-and-shelley-translation.md`, the PPUP half of the Shelley-PPUP/POOLREAP entry (now [[shelley-ppup-votedfutureparams-verified]], see Governance section above), and the MIR-witness half of the BBODY entry below (now [[shelley-genesisdeleg-and-mir-witness-quorum]]) — the POOLREAP future-pool-params-adoption half and the BBODY total-ExUnits-scope/GenesisDelegCert-named-key half are still missing.
- [BBODY total-ExUnits scope + MIR/GenesisDeleg witness rules](alonzo-bbody-exunits-and-cert-witnesses.md) — MISSING FILE, unverified summary only (MIR-witness-quorum half now RESTORED+corrected, see [[shelley-genesisdeleg-and-mir-witness-quorum]] below): maxBlockExUnits sums ALL txs incl. IsValid=False; GenesisDelegCert needs the named genesis key
- [GenesisDelegCert FutureGenDeleg maturation (1x stabilityWindow) + MIR quorum-witness exact semantics (live-verified 2026-07-06)](shelley-genesisdeleg-and-mir-witness-quorum.md) — corrects prior "MIRCert needs coreNodeQuorum" one-liner: maturation delay is `stabilityWindow` (3k/f) ONCE not 2x (2x is a different mechanism, PPUP submission deadline/HFC point-of-no-return); `adoptGenesisDelegs` runs every TICK (per block) not just epoch boundary, `s<=slot` partition; `validateMIRInsufficientGenesisSigs` intersects witness set against genDelegs map VALUES (delegate/hot key hash) not keys, keyed on dsGenDelegs only (not future), quorum=static sgUpdateQuorum; enforced Shelley-Babbage, structurally absent in Conway (babbageUtxowMirTransition dropped, AtMostEra "Babbage" type constraint). See [[project_dugite_genesisdeleg_mir_gaps_2026_07_06]] for 2 dugite gaps found alongside this.
- [POOLREAP future-pool-params adoption](shelley-ppup-votedvalue-and-poolparams-adoption.md) — MISSING FILE, unverified summary only (PPUP half of this old entry is now RESTORED, see [[shelley-ppup-votedfutureparams-verified]] above): psFutureStakePoolParams adoption is a drop-missing Map.merge inside POOLREAP (future-only keys are DROPPED not added); minUTxOValue was a normal updatable PParams field
- [Byron duplicate-TxIn handling + Byron→Shelley translation pot values](byron-duplicate-inputs-and-shelley-translation.md) — RESTORED + re-verified live 2026-07-06: translateToShelleyLedgerStateFromUtxo zeroes utxosFees/utxosDeposited/casTreasury/ssFee (emptySnapShots); Byron never had a fee-pot accumulator to carry forward; reserves = maxLovelaceSupply - sumCoinUTxO(translated, zero-value-TxOuts-filtered) implicitly absorbs all burned Byron fees

## CBOR Set-Duplicate Decoding
- [Duplicate TxIn in `inputs`/`collateral`/`referenceInputs` — decode-level PV9 gate, not a Phase-1 predicate (live-verified 2026-07-29)](set-txin-duplicate-decode-pv9-gate.md) — generic `DecCBOR (Set a)` instance = `decodeSet decCBOR`; PV>=9 (Conway+) `decodeSetEnforceNoDuplicates` FAILS decode on any dup (count-vs-Set.size mismatch, surfaces as `DecoderErrorDeserialiseFailure`, NOT a `SubmitFail`/predicate-failure); PV2-8 (Shelley-Babbage) `Set.fromList` silently dedups, decode succeeds, no Phase-1 check ever existed for this (structurally unrepresentable post-decode either way); separate `decodeAnnSet` Annotator-Set path has its OWN later PV12 gate, irrelevant to TxIn fields specifically (confirmed `From`/plain decCBOR in Shelley/Alonzo/Conway TxBody source)

## Phase-2 Script Collection Errors
- [CollectError(NoCostModel) exact mechanics — construction, hard-reject-regardless-of-isValid, per-script language keying, native-script exclusion (live-verified 2026-07-06)](nocostmodel-collecterror-exact-mechanics.md) — `apply` in Alonzo Plutus/Evaluate.hs: `maybe (Left (NoCostModel lang)) Right $ Map.lookup lang $ costModelsValid costModels`; Conway CollectErrors hard-rejects via `(?!:)`/`whenFailureFree` BEFORE isValid branch in both Babbage `expectScriptsToPass`/`babbageEvalScriptsTxInvalid` (no Conway override); per-script `plutusScriptLanguage` keying (V2-only tx never touches V1 cost model); absence-vs-wrong-length are disjoint (short cost models padded to maxBound before map insertion); native scripts filtered out by `toPlutusScript`/`lookupPlutusScript` returning Nothing, never reach cost-model lookup; ordering gotcha: `gg` fold short-circuits so later scripts' own NoCostModel can be masked by an earlier script's different error

## Phase-1/UTXOW Witness + Script Exactness (live GitHub-verified 2026-07-04)
- [evalTimelock semantics + cert script-witness rules](timelock-and-cert-script-witnessing.md) — MISSING FILE, unverified summary only: RequireTimeStart/Expire evaluated vs tx body ValidityInterval not chain slot; SNothing bound = automatic FAIL (not vacuous pass) for both; per-constructor scriptsNeeded for Conway/Shelley TxCerts (only deposit-less ConwayRegCert/ShelleyRegCert skip the witness)
- [ppViewHashesMatch / mkScriptIntegrity exact carve-out + babbageMissingScripts](utxow-script-integrity-and-witness-checks.md) — MISSING FILE, unverified summary only: proof no scriptIntegrityHash-present+zero-redeemer+zero-datum tx is ever valid in any era; PV11 renames PPViewHashesDontMatch→ScriptIntegrityHashMismatch; extraneous NATIVE witness scripts also trigger ExtraneousScriptWitnessesUTXOW
- [Plutus script CBOR+Flat deserialise, V1/V2 vs V3 RemainderError asymmetry](plutus-script-cbor-flat-deserialise.md) — MISSING FILE, unverified summary only: must be CBOR bytestring wrapping Flat bytes (serialise . SerialiseViaFlat); trailing-garbage RemainderError only enforced for V3+, not V1/V2; **dugite-uplc Program::from_cbor/from_flat currently doesn't replicate the V3 remainder check (confirmed gap)**
- [Babbage/Conway UTXO exact computations](babbage-conway-utxo-exact-computations.md) — MISSING FILE, unverified summary only: supplemental datum hashes include reference-input UTxOs; ref-script-size Set.union dedups shared inputs; maxValSize serSize is exact PV-aware CBOR length; PlutusV3 DatumHash (not NoDatum) still requires the datum witness (CIP-0069 only exempts NoDatum)

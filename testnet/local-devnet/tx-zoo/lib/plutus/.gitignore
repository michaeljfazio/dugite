# Generated by ../build-plutus.sh — do not commit, do not hand-edit.
#
# These envelopes are derived deterministically from
# tests/conformance/upstream/plutus-examples.json (IntersectMBO's own
# plutus-tx-compiled scripts) and every one is verified against the ScriptHash
# cardano-ledger recorded beside the bytes before the zoo uses it.
#
# The JSON is the single source of truth. Tracking the derived envelopes as
# well would let a hand-edited one silently override the vendored bytes, which
# is exactly the drift the hash check exists to prevent — and before #969/#970
# five of them WERE tracked, alongside a `.v3-handler-set` cache marker.
*
!.gitignore
